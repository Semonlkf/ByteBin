server:
  port: 8080
spring:
  servlet:
    multipart:
      max-request-size: 5GB  # 上传文件总的最大值 默认10MB
      max-file-size: 1GB #单个文件最大值 默认10MB
  datasource:
    jdbc.driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demo?userUnicode=true&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 1234
    druid:
      initial-size: 2
      max-active: 8
      min-idle: 1
      max-wait: 6000
      stat-view-servlet:
        enabled: true
        login-username:
        login-password:
      web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: '*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*'
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      password: 1234
      timeout: 6000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1
  web:
    resources:
      static-locations: file:D:\\project\\Bloggy\\static
  mail:
    protocol: smtps
    host: smtp.163.com
    port: 587
    username: lxxsly2021@163.com
    password: GGFLSEULCOXZOZZC
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          timeout: 10000
          localhost: 8082
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            enable: false
          socketFactory:
            port: 587
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/*Mapper.xml
  global-config:
    db-config:
      logic-delete-value: 0
      logic-not-delete-value: 1
sa-token:
  is-log: true

